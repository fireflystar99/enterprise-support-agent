version: v3
description: "Hybrid retrieval + grounding guard with mandatory citations"
retrieval:
  mode: hybrid
  top_k: 8
  rerank_top_n: 3
grounding:
  enabled: true
  mandatory_citations: true
  access_filter: true
